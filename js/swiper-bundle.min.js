/**
 * Swiper 4.0.7
 * Most modern mobile touch slider and framework with hardware accelerated transitions
 * http://www.idangero.us/swiper/
 *
 * Copyright 2014-2017 Vladimir Kharlampidi
 *
 * Released under the MIT License
 *
 * Released on: November 28, 2017
 */
<script>
  const swiper = new Swiper('.project-slider', {
    slidesPerView: 1,
    spaceBetween: 30,
    pagination: {
      el: '.swiper-pagination',
      clickable: true,
    },
  });

  // Cible les éléments du texte
  const titleEl = document.getElementById('project-title');
  const descEl = document.getElementById('project-desc');
  const specsEl = document.getElementById('project-specs');

  // Fonction pour mettre à jour le texte
  function updateProject(slide) {
    const title = slide.dataset.title;
    const desc = slide.dataset.desc;
    const specs = JSON.parse(slide.dataset.specs);

    titleEl.innerHTML = title;
    descEl.innerHTML = desc;

    // Reconstruire le tableau
    let html = '';
    for (let key in specs) {
      html += `<tr><td>${key.toUpperCase()}</td><td>${specs[key]}</td></tr>`;
    }
    specsEl.innerHTML = html;
  }

  // Au changement de slide
  swiper.on('slideChange', () => {
    const currentSlide = swiper.slides[swiper.activeIndex];
    updateProject(currentSlide);
  });

  // Initialisation
  updateProject(swiper.slides[swiper.activeIndex]);
</script>